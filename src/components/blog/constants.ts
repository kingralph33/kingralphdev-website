/**
 * Blog component constants
 */

/**
 * Debounce delay for search input in milliseconds
 * This controls how long to wait after user stops typing before triggering search
 */
export const SEARCH_DEBOUNCE_DELAY = 300;
